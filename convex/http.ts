import { httpRouter } from "convex/server";

const http = httpRouter();

// TODO: add HTTP routes for external webhook endpoints if needed
// Example: http.route({ path: "/webhook", method: "POST", handler: ... });

export default http;
